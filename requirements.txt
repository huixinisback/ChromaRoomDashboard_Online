# Core dependencies for ChromaRoom Dashboard
streamlit>=1.28.0
pandas>=2.0.0
matplotlib>=3.7.0

# Note: sqlite3, json, re, os, and typing are built-in Python modules
# and do not need to be installed separately
